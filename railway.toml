[build]
builder = "nixpacks"

[deploy]
# Web server + embedded APScheduler — runs continuously as a web service
# Daily discovery fires at 08:00 Buenos Aires (ART = UTC-3) via BackgroundScheduler
startCommand = "python run.py --web"

[[deploy.envVars]]
name = "PYTHONUNBUFFERED"
value = "1"
